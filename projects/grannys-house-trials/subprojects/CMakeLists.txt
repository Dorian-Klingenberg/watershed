# Future focused slices can be added here.
#
# For now, each subproject should link the single shared project library instead
# of creating more compiled subsystem targets.

add_subdirectory(grass-field-001)
